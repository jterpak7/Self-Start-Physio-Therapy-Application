import { BookingsDirective } from './bookings.directive';

describe('BookingsDirective', () => {
  it('should create an instance', () => {
    const directive = new BookingsDirective();
    expect(directive).toBeTruthy();
  });
});
